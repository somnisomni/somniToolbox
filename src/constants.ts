import packageJson from "@/../package.json";

const APP_NAME = "somni::Toolbox";
const APP_NAME_HTML = "somni::<strong>Toolbox</strong>";
const APP_VERSION = `v${packageJson.version}`;

export {
  APP_NAME,
  APP_NAME_HTML,
  APP_VERSION,
};
